import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ravunana',
  appName: 'hinarioApp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
